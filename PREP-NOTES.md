# public-prep branch — review notes

Prepared 2026-07-20 from the pre-publication privacy audit. Delete this file before (or when) merging.

## What changed on this branch

**Removed (kept private — they exist on `main`):**
`daily-job-search-crawl`, `weekday-morning-brief`, `weekly-system-self-audit`, `poker-hud-advisor`, and the third-party job-search suite (`job-search`, `job-search-setup`, `cover-letter`, `network-scan`). Their index nodes, edges, manifest entries, buckets (`job-search`, `projects`), and plugin dirs are gone; everything regenerated cleanly.

**Sanitized (swapped in the versions built for skills-library-public, plus new scrubs):**
- 8 market-analysis skills — generic `analysis-rules.md`, scrubbed templates, machine-specific cron scripts dropped
- `assistant-heartbeat`, `meeting-reminder`, `memory-maintenance`, `daily-legal-workflow-app-ideas` — sanitized versions
- `morning-brief`, `evening-wrap`, `weekly-wrap` — OpenClaw cron scripts dropped, source lines generalized
- `morning-music-alarm` — speaker IP and log path genericized in the script
- `daily-brief-export` — script renamed, auth header/env generalized, personal sender blocklist replaced with a placeholder comment

**Added:**
- `skill-distiller` (new first-party meta-skill, agent-infrastructure bucket)
- `tools/privacy_sweep.py` — pre-publish gate; run it before every push, or wire it into CI next to the drift audit

**Regenerated by `tools/build.py`:** dispatcher (77 skills), 8 bucket plugins + marketplace.json (v0.2.0), README tables, graph.html (99 nodes / 145 edges).

## Deliberate keeps — confirm you're comfortable

1. **Sefer / Bari / OpenClaw as provenance** in index.json source nodes, the README provenance table, and dispatcher dependency notes. No endpoints, tokens, or schedules — just the story of where skills came from. It reads well; scrub further only if you want the agent names private.
2. **Your email in `.claude-plugin/*.json` and `tools/build_plugins.py`** as plugin author metadata. Standard for a public marketplace; swap for a GitHub-noreply address if you prefer.
3. **System nodes in index.json/graph** (Daily Brief app, Telegram bot, Sonos, StreamEZ stack) as architecture documentation — descriptions only, no URLs.

## To publish

```bash
cd ~/dev/Skills
git checkout public-prep
python3 tools/privacy_sweep.py      # must print CLEAN
git diff main --stat                # review scope
# when satisfied:
git checkout main && git merge public-prep
git push origin main
# then flip the repo to Public in GitHub Settings → Danger Zone
```

`skills-library-public` (in AI Skills) is superseded by this — retire it or keep it as a curated excerpt.
