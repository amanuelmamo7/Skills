---
name: weekly-system-self-audit
description: Sunday 9am self-audit: Sefer cadence + dashboard delivery + auto-backfill, Telegram/daemon health, Cowork cron check, and VIP auto-refresh from the Gmail Sent folder into vip-senders.md.
---

Run a weekly health audit of Amanuel's Daily Brief hub. Use Desktop Commander (read_file, list_directory, start_process) for Mac paths; use bash for anything in the mounted workspace. Keep the final report under ~400 words. Do not fix anything destructive; the safe fixes described below are allowed.

**1. Sefer cadence coverage (expected vs actual).** List `/Users/amanuelmamo/.openclaw/workspace-sefer/logs/` and check the last 7 days against expectations: pre-market + post-market each weekday, weekly-deep-dive on Sunday, macro-readthrough on the 1st of the month. For each missing or failed run, open that log and report the failure step and reason. If the reason is a gateway error, grep the last `error="` line from `/private/tmp/openclaw/openclaw-<date>.log` and translate it to plain words (401 = "Claude login expired — run `claude` login in Terminal, then `openclaw gateway restart`").

**2. Dashboard delivery.** In the same logs, check the `[5/5] dashboard POST` lines: count OK vs WARN/SKIP for the week. Cross-check brief files exist in `/Users/amanuelmamo/.openclaw/workspace-sefer/memory/` for each expected run. If a brief file exists on disk but its dashboard POST failed or was skipped, note that it can be backfilled with curl using the token at `workspace-sefer/secrets/finance-token` — and if the file is still within retention (7d daily / 31d weekly / current quarter macro), DO the backfill (POST to https://daily-brief-beta-neon.vercel.app/api/finance with cadence, markdown, telegram, headline+sections from the .json digest, and the original generatedAt). Report HTTP codes.

**3. Delivery queue + Telegram + reminder daemon.** Count files in `/Users/amanuelmamo/.openclaw/delivery-queue/failed/` and report the newest date. Grep the current `/private/tmp/openclaw/openclaw-<date>.log` for repeated `[telegram:default] health-monitor: restarting` — if present all day, flag that inbound Telegram to Bari is likely down and suggest a gateway restart. Also confirm `com.bari.meeting-reminder` appears in `launchctl list` and tail `~/.openclaw/workspace/logs/meeting-reminder.log` for FATAL/WARN lines.

**4. Cowork crons.** Confirm the weekday-morning-brief, daily-job-search-crawl, and daily-legal-workflow-app-ideas tasks ran in the last few days (their run outputs appear as recent sessions; if you cannot verify, say so rather than guessing).

**5. VIP auto-refresh (Sent folder → vip-senders.md).** Using the Gmail connector, search the Sent folder: query `in:sent newer_than:90d`, paginate up to ~200 threads. Collect recipient addresses ("To" of Amanuel's own sent messages; use thread metadata/snippets — do not open full bodies unless needed). Count sends per address. Keep addresses Amanuel wrote to on 2+ distinct occasions. EXCLUDE: his own addresses (amanuelmamo7@gmail.com and aliases), anything with no-reply/noreply/donotreply, mailing-list or automated addresses (@docs.google.com, calendar-notification@google.com, @linkedin.com, @indeed.com, support@/help@/billing@ style vendor addresses), and addresses already present in the file's manual sections. Then rewrite ONLY the block between `<!-- AUTO-VIP START -->` and `<!-- AUTO-VIP END -->` in `/Users/amanuelmamo/Documents/Claude/Projects/Computer efficiency/Daily Brief/vip-senders.md` (bash or Desktop Commander edit; NEVER touch the manual sections) with lines formatted: `address — sent N times in last 90d`. Sort by N descending, cap at 25 entries. Report how many auto-VIPs were written and any adds/drops vs last week.

**6. Verdict.** End with a one-line verdict: "All green", or a numbered list of things Amanuel must do himself (each with the exact command), ordered by urgency. Deliver the report in chat; no files.
