You are Sefer. Today is Sunday. Run a weekly deep-dive.

FIRST — write a resume-protocol breadcrumb.
Before reading the queue or any fetches, append this exact line to today's
daily log (memory/$(date +%Y-%m-%d).md), creating the file if it doesn't
exist:
  [intent] weekly deep-dive — selecting target + filling thesis/justification template, ~15 min, output memory/<YYYY>-W<WW>-deep-dive.md
Deep-dives are the longest-running cron Sefer runs — they touch many
sources and reason at length. The watchdog risk is highest here. This
breadcrumb lets the next session resume cleanly (per AGENTS.md §1A Resume
Protocol). Only proceed after the breadcrumb is on disk.

Target selection:
1. Read memory/deep-dive-queue.md if it exists. Take the topmost unprocessed target.
2. If the queue is empty or missing, scan the past 7 days of pre-market and post-market briefs in memory/. Pick the watchlist name mentioned most often (or the largest anomaly flagged) and label the output "auto-selected".

Template choice based on ISO week number:
- Odd ISO week → fill templates/company-thesis.md
- Even ISO week → fill templates/trend-justification.md

Output — write all three files, then return the full template as your reply:
1. Write filled template to memory/<YYYY>-W<WW>-deep-dive.md (ISO year + week).
2. Write a Telegram-formatted summary (≤ 2000 chars, with /sefer prefix) to memory/<YYYY>-W<WW>-deep-dive-telegram.txt. The cron script will POST this to the bot.
3. Write a JSON digest to memory/<YYYY>-W<WW>-deep-dive.json (same ISO year+week). This powers the Daily Brief web dashboard's Weekly panel (the .md is the full thesis). The cron script POSTs it. MUST be valid JSON (no trailing commas, no comments) with exactly this shape:

{
  "headline": "the one-line take, ≤240 chars; lead with the **TICKER** and **bold** the core claim",
  "sections": [
    { "type": "stats", "title": "Snapshot", "items": [
      { "label": "Price", "value": "$416.67", "direction": "flat" },
      { "label": "Mkt Cap", "value": "$3.10T", "direction": "flat" },
      { "label": "Fwd P/E", "value": "22.5x", "direction": "flat" },
      { "label": "Off 52w high", "value": "-25%", "direction": "down" }
    ]},
    { "type": "bullbear", "title": "Thesis", "bull": ["Azure re-accelerated to +40%","Commercial RPO +99% — two years pre-contracted"], "bear": ["Q4 guide was light on margin","$190B capex could keep FCF compressed 2-3 yrs"], "verdict": "Undershoot — fundamentals far above the ~6.5% terminal FCF growth priced in; re-rates when FCF margins inflect." },
    { "type": "list", "title": "Catalysts (90 days)", "items": ["~Jul 29 Q4 FY26 earnings — Azure growth % is the number that controls the multiple","FOMC Jul 29 / Sep 16 — rate path drives WACC"] },
    { "type": "callout", "title": "What Would Change My Mind", "tone": "neutral", "body": "**bull invalidation:** Azure <35%. **bear invalidation:** Q4 beat + FY27 capex moderation signal." }
  ]
}

Rules: this is a single-name deep-dive, so the bullbear panel is the centerpiece. direction is "up"/"down"/"flat". Mirror the .md — the digest is the highlights. Omit any section you lack data for. Allowed section types: stats, table, movers, callout, bullbear, list — unknown types are ignored.

Operate under SOUL.md and AGENTS.md. End with Sources & timestamps footer.
